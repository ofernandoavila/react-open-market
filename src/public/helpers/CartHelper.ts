import { CartItemType } from "../../types/CartTypes";
import { ProductType } from "../../types/ProductTypes";

export function DeleteFromCart() {
    function handleDeleteFromCart(cart: Array<CartItemType>, produto: ProductType ) {
        
    }
}